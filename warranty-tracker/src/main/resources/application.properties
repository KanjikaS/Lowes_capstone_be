spring.application.name=warranty-tracker
# === PostgreSQL Database Configuration ===
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=root
spring.datasource.driver-class-name=org.postgresql.Driver

# === JPA & Hibernate ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# === Server Port ===
server.port=8080

# === JWT Configuration ===
jwt.secret=a-very-strong-and-secure-secret-key-of-32-characters
jwt.expiration=36000000

# === File Upload Configuration ===
spring.web.resources.static-locations=classpath:/static/,file:uploads/

# === Email Configuration ===
# Development configuration (replace with your actual Gmail credentials)
spring.mail.host=smtp.gmail.com
spring.mail.port=587

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.username=serviceprolowes@gmail.com
spring.mail.password=vssccticizxezbpd

# === Scheduling Configuration ===
spring.task.scheduling.pool.size=2